dest:
  driverName: sqlite3
  descriptor: test/data/db1.db
  table: events_clone
  pseudoColumns:
    - name: hour
      expression: strftime('%Y-%m-%d %H', ts)
    - name: date
      expression: date(ts)

source:
  driverName: sqlite3
  descriptor: test/data/db1.db
  table: events
  pseudoColumns:
    - name: hour
      expression: strftime('%Y-%m-%d %H', ts)
    - name: date
      expression: date(ts)

uniqueColumns:
  - id
partition:
  columns:
    - date
