package sync

import (
	"testing"
)

func Test_Server(t *testing.T) {

	/*
		config := &Config{
			Debug:                true,
			ScheduleURL:          "",
			ScheduleURLRefreshMs: 10000,
		}
		service, err := New(config)
		if err != nil {
			log.Fatal(err)
		}
		server := NewServer(service, 8085)
		server.ListenAndServe()

	*/
}
